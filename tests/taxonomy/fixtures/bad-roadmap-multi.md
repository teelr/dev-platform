# Test Roadmap (bad — multiple killed prefixes in one file)

Exercises the accumulator loop: a single file must surface BOTH a Sprint
killed-prefix AND a legacy R-prefix in the same scan pass. Should fail.

- **v0.1: Foundation** *(complete)* — valid
- **Sprint K: Communication Channels** *(planned)* — KILLED: Sprint prefix
- **R7: Team Enablement** *(planned)* — KILLED: legacy R-prefix
- **v1.0: Feature-complete** *(planned)* — valid
