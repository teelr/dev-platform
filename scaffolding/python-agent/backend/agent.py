"""Agent logic for {{PROJECT_NAME}}.

Replace `run_once` with the actual agent flow. The current implementation is a
no-op placeholder so the smoke test (`python main.py`) passes after scaffold.
"""
from __future__ import annotations


def run_once() -> dict[str, str]:
    """One-shot agent call. Replace with real logic.

    TODO: instantiate kermit-harness runtime and run the agent.
    """
    return {"status": "ok", "note": "replace this with real agent logic"}
