# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
bin/
{{PROJECT_NAME}}

# Test + coverage
*.test
coverage.out
coverage.html

# Go workspace
go.work
go.work.sum

# Vendored deps (we use modules; vendor/ is opt-in)
vendor/

# Env / secrets — never commit
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
.idea/
*.swp
*~

# Build artifacts
dist/
build/
