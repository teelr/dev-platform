# v1.7 Plan-Time Isolation — **closed** (2026-07-01

v1.7 Plan-Time Isolation — **closed** (2026-07-01, PR #41): moves branch/worktree creation from `/code` time to `/plan` time — `/plan` derives the feature slug, creates the branch (or worktree, for opted-in multi-chat projects) as soon as the user decides what to build, and renames the current tmux window to match; `/code`'s own branch-creation step is now documented as the fallback for hand-authored specs. Adds `shell/new-session.sh`, a CLI+tmux helper for manually starting a second parallel worktree + tmux window + `claude` session. Built conversationally, no `tasks/*-spec.md` (same shape as the "Workflow redesign chore" precedent). `/review` caught one real bug before commit: worktree-mode instructions captured `MAIN` after `EnterWorktree` had already re-rooted the session — fixed to capture it as an explicit first action, matching `/code`'s existing ordering. Gate at 197 PASS (no new suite — instruction-file content, not new deployable code). Released `v1.7`, milestone closed.
