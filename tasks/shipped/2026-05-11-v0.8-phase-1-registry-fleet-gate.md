# v0.8 Phase 1 — Registry + Fleet Gate

v0.8 Phase 1 — Registry + Fleet Gate, **opens v0.8** (2026-05-11, PR #12 squash-merged as commit `85ebd38`): `monitoring/projects.json` (5-project registry: dev-platform + atlas + kermit + kermit-pa + keystone) + `scripts/fleet-gate.sh` (~264 lines, parallel sweep + per-project timeout + `fleet_gate_run` telemetry) + `tests/fleet-gate/` (mock project tree + 10-assertion runner). **Real-world finding from live sweep**: 4 PASS + 1 FAIL (kermit's conda env lacks pytest — project-side environmental drift, exactly what v0.8 exists to surface). Two spec deviations backfilled into the spec's "What NOT to Do": (a) renamed fixture dir `projects/ → mock-projects/` to dodge the unanchored `.gitignore:132 projects/` exclude that beats the earlier `!tests/**/*.sh` re-include by rule-order; (b) extended `scripts/gate_fast.sh`'s find with `! -path "*/fixtures/*"` so auto-discovery doesn't pick up fixture `.sh` files as test runners.
