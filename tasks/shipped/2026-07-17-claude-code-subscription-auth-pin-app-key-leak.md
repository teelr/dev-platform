# Claude Code subscription-auth pin + app-key leak detection (2026-07-17, PR #53): a…

Claude Code subscription-auth pin + app-key leak detection (2026-07-17, PR #53): a project's `ANTHROPIC_API_KEY` (meant for its own Anthropic API usage) was leaking into VSCode's integrated terminal via `python.terminal.useEnvFile`, which Claude Code auto-detects and silently bills to instead of the Claude subscription. Fix: `forceLoginMethod: "claudeai"` in `settings/managed-settings.json`, deployed to `/etc/claude-code/managed-settings.json` via a new `install.sh managed` category (sudo, copy-deployed, drift-checked by `verify.sh`) — managed settings outrank env-var auth machine-wide. `scripts/check_env_leak.sh` audits every project for the leak vector itself; found it live in kermit-v3 and keystone (fix deferred to their own sessions). Gate at 212 PASS.
