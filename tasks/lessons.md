# Lessons Learned

Patterns from corrections. Reviewed at session start. Consolidated into CLAUDE.md rules when 2-3 similar entries emerge.

## Active Lessons

| Date | Lesson | Project | Status |
| ---- | ------ | ------- | ------ |
| 2026-03-18 | Each project needs its own port series — NVR on 8001 conflicted with Kermit | RICH_NVR | → Rule in dev CLAUDE.md |
| 2026-05-08 | Before a "copy artifacts from external system" spec, audit the source. Foundation Spec assumed `~/.claude/keybindings.json` existed (it didn't), assumed `skills/<name>/` subdirs were populated (they were empty), and treated settings.json as commit-ready (it had 3 plaintext DB passwords + 11 junk one-off entries). Reality only surfaces by reading the source path before /code starts. Three deviations approved at /code intake; would have been three commits to fix if caught later. | dev-platform | active |
| 2026-05-08 | install/deploy scripts MUST refuse to clobber real files. install.sh's `link_file` errors when the target is a real (non-symlink) file, forcing the user to back up + remove first. Without this guardrail, the first install of a symlink-based deploy silently destroys user data — the exact failure mode of every "rename your config dir before installing" tool. | dev-platform | active |
| 2026-05-09 | Hook scripts that read external-tool event payloads MUST degrade gracefully on shape change. The R1.5 heartbeat hook reads Claude Code's PostToolUse JSON via `python3 -c '...try/except...'` and falls back to `tool=?` if the payload schema differs from expectation. The schema is unverified at spec time — Claude Code may evolve its event format. Without try/except, a schema bump would break every tool call's hook silently. Pattern: hooks consuming third-party payloads always use defensive parsing, never assume keys exist. | dev-platform | active |
| 2026-05-09 | /docs MUST NOT write commit hashes for the spec being shipped this session. The chicken-and-egg paradox: /docs runs BEFORE the bundled commit (per Docs Before Commit rule) but the hash for that bundled commit doesn't exist until the commit lands. R1 + R1.5 each forced a small backfill commit (e12a008, e0ba399) two days running. Fix landed in commands/docs.md: planning.md "Recently shipped" entries are now hash-free; `git log` is the authoritative hash record. Also fixed in same pass: the /docs Step 5 was telling people to commit docs separately, conflicting with the bundling rule from dev/CLAUDE.md. /docs now only stages — the bundled feat commit takes both. | dev-platform | → Rule in commands/docs.md |
| 2026-05-10 | R4a spec missed two implicit prerequisites that surfaced at /code time: (a) `.gitignore` allow-list extension for template file types (.ts, .tsx, .py, .go, .toml, .mod, .gitkeep, etc.) plus subdirectory re-includes (`!scaffolding/**/`) — without these, scaffolded template content was silently gitignored; (b) `new-project.sh` `find` pattern initially missed `.gitignore` (no extension), so the go-service template's `.gitignore` placeholder didn't get substituted. Both caught + fixed at /code time, but the spec assumed they'd "just work." Class of issue: spec writers (including /plan agents) routinely under-spec gitignore + sed-pattern coverage when introducing new file types. Going forward: any /plan that adds a new file type or a new top-level dir MUST explicitly include a Change for the gitignore allow-list update AND verify the substitution coverage. | dev-platform | active |
| 2026-05-10 | R3 Testing should ship BEFORE R2 Monitoring. Original ROADMAP had R2 next (telemetry on workflow effectiveness, leveraging the R1.5 heartbeat). Reality after R4a: every dev-platform "gate fast" run is a Bash one-liner cobbled together in conversation — taxonomy, syntax, JSON validity, install round-trip, live verify, heartbeat smoke, scaffold smoke, edge cases. No `scripts/gate_fast.sh` exists. Without R3, every spec from here on (R5 migration, R4b VSCode, additional templates) ships with conversation-derived gate coverage. R3 *prevents* drift; R2 only adds *visibility* to drift. Prevention is higher-leverage than visibility when neither exists yet. R3 promoted ahead of R2 in ROADMAP.md on 2026-05-10. | dev-platform | → Rule in ROADMAP.md ordering |
| 2026-05-10 | Bash helpers that maintain state across subshells need a shared file or env-var mechanism. R3's first `gate_fast.sh` ran each test suite via `bash "${runner}"` (subshell), and each runner's `PASS_COUNT` / `FAIL_COUNT` lived in its own subshell scope — the orchestrator's totals were ALWAYS the 5 lift checks alone, never the 37 suite results underneath. Caught only because /code re-ran gate_fast.sh and saw "5 PASS" when 42 was expected. Fix: shared `_GATE_COUNTS_FILE` (mktemp), exported env var, assert.sh appends to it, orchestrator reads at end. Pattern: anything maintained "across subshells" via in-memory counters silently breaks; use a shared sink. | dev-platform | active |
| 2026-05-10 | Docs that promise behavior the code doesn't deliver violate the Honesty rule. R3 `tests/README.md` told future contributors "the orchestrator discovers new runners automatically by walking `tests/<suite>/`" — but the orchestrator had a hardcoded suite list (`for suite in hooks commands taxonomy install scaffold; do`). Adding `tests/foo/` wouldn't auto-discover; user would also need to edit gate_fast.sh. Caught by /review. Two fix shapes: tighten the README (match the code) or loosen the code (match the README). Picked the latter — auto-discovery is the better long-term contract. Pattern: when /review surfaces a doc-vs-impl mismatch, ALWAYS prefer fixing the implementation to match the doc's promise unless the promise was itself wrong. | dev-platform | → Captured in Honesty rule (existing) |
| 2026-05-11 | Specs that change a script's output contract MUST include a Change that updates the regression-test suite asserting that contract. v0.5 Phase 1 Change 2 migrated `hooks/post-tool-heartbeat.sh` from text format (`tool=Bash`) to JSONL, but the spec didn't list updating `tests/hooks/post-tool-heartbeat/run.sh` (the v0.4 regression suite) as a Change. The omission surfaced when gate_fast went red mid-/code with 4 FAILs on the heartbeat fixture suite — implicit, not explicit. Pattern: when a /plan Change rewrites the output format / API / event shape of an artifact that has a downstream test, the spec MUST also list a Change to update that test in lockstep. Going forward, /plan reviews check every output-changing Change for a paired test-update Change. | dev-platform | active |
| 2026-05-11 | Per-Spec-Phase branching strategy adopted for v0.5 onward. Each Spec Phase (typically 2–5 Changes) → one feature branch (`v0.5/phase-1-schema-storage` etc.) → one PR assigned to the matching GitHub Milestone → squash-merge to main. Reviewed in /review session 2026-05-11; alternatives considered: per-Change PRs (too granular, 14 PRs for v0.5), per-Roadmap-Phase PR (too large, ~250-line aggregator + 4 hooks + tests in one diff), stay on main (no review surface for 2–10 dev team). Per-Spec-Phase is the Goldilocks granularity. Tooling enforcement (required CI status check on the taxonomy + format) ships in v0.7 Team Enablement. | dev-platform | active |
| 2026-05-11 | When you introduce a new file type into a directory that's governed by glob-based loops, audit ALL consumers in the same /code session — don't ship the file and hope the loops pick it up. v0.5 Phase 2's /review refactor added `hooks/_emit_event.py` (first `*.py` ever under `hooks/`). Required parallel updates to 4 files: (a) `.gitignore` allow-list (`!hooks/*.py`), (b) `scripts/install.sh` (add `*.py` to the deploy glob), (c) `scripts/verify.sh` (add `*.py` to the symlink-check loop), (d) the hooks/ directory README (none exists for hooks yet but worth flagging). Missing any of these breaks deploy silently — install would skip the file, verify wouldn't catch the gap, hooks would fall back to degraded behavior. Pattern: any new top-level file extension OR any new file in a glob-managed directory triggers a 4-point consumer audit before commit. | dev-platform | → Rule in `dev/CLAUDE.md` |
| 2026-05-11 | When /review surfaces "inconsistent X across N files," the answer isn't always "align them." Sometimes the asymmetry is deliberate and the fix is to document the rationale where readers look. v0.5 Phase 2's /review flagged inconsistent fallback-emission strategies across 4 hooks (session-start + post-tool-heartbeat have fallback echo; user-prompt + pre-tool-use are silent). Each has a defensible reason (privacy for user-prompt; pairing semantics for pre-tool-use). The fix was a "Fallback asymmetry" section in `monitoring/README.md`, NOT alignment. Pattern: when /review's QUALITY findings call out inconsistency, first ask "is the asymmetry deliberate?" — if yes, document the rationale; if no, align. | dev-platform | active |
| 2026-05-11 | Synthetic test fixtures must use PAST timestamps. v0.5 Phase 3's /test built an end-to-end fixture covering every metric path — initially using timestamps like `2026-05-11T09:00:00` while current local time was ~05:00. The aggregator's window filter (`ts > until`, where `until = now`) silently dropped all 12 fixture events. Surfaced as "event_count = 0" in the first assertion. Fix: backdate fixture timestamps (e.g., `2026-05-10T...`) so they fall inside any reasonable `--period` window. Pattern: when writing time-bounded test fixtures, anchor timestamps to a known-past date (or compute relative to "now" minus an offset); never use today-but-future times. The aggregator's `--period all` does NOT mean "ignore the upper bound" — it means `since=datetime.min`, but `until` is always `now`. | dev-platform | active |
| 2026-05-11 | Spec-faithful implementation can ship inconsistencies inherited from the spec itself. v0.5 Phase 3's /review flagged two cross-metric naming/structural inconsistencies (`code_invocations` vs `count`; `events_per_project` inline vs as a metric_* helper) — BOTH of which were literally what the spec prescribed. The fixes were correct (align to `count`; extract the helper), but the deeper lesson is: when /code implements a spec verbatim, /review should still audit for consistency that the spec might have missed. /plan agents are not infallible. Pattern: /review's QUALITY pass looks at the implementation in its full repo context, not just against the spec. Findings that contradict the spec are valid when consistency with the broader codebase wins; flag them as "spec deviation worth absorbing into the next spec revision" rather than treating the spec as immutable. | dev-platform | active |
| 2026-05-11 | Consumer-audit pattern recurred — 2nd instance — Phase 2 added `hooks/_emit_event.py` (first `*.py` under `hooks/`); Phase 4 added `tests/monitoring/asserts.py` + `*.jsonl` fixtures (first `*.py` and `*.jsonl` under `tests/`). Same root cause both times: glob-based loops in consumer scripts + per-extension gitignore allow-lists silently drop new file types unless explicitly allowed. Both caught at QC time, but the recurrence triggered promotion to a `dev/CLAUDE.md` rule per the Boris Cherny consolidation guideline. | dev-platform | → Rule in `dev/CLAUDE.md` |
| 2026-05-11 | Consumer Audit rule (just promoted to `dev/CLAUDE.md`) fired TWICE within v0.6's same /code session — within hours of the rule landing. (1) `extensions/vscode/` was the first subdirectory of `extensions/` to contain real files; gitignore had `!extensions/**/*.json` and `!extensions/**/*.md` but was missing `!extensions/**/` subdir re-include (same pattern as scaffolding/monitoring/tests/). Caught at Change 1 acceptance via `git check-ignore -v`. (2) `tests/vscode/fixtures/mock-bin/code` was the first extension-less file under `tests/` — none of `!tests/**/*.{sh,md,py,json,jsonl,txt}` matched. Caught when adding the round-trip tests for /review fix #3. The rule paid for itself THREE times in roughly 24 hours (Phase 2 + Phase 4 + v0.6 ×2). Pattern: extensions to glob-managed dirs continue to surface this; the audit-at-/code-time approach is reliable; the cost is ~30 seconds per occurrence; the absence of the rule would have meant two broken-on-fresh-clone PRs in v0.6 alone. | dev-platform | active — validates the rule |
| 2026-05-11 | Mock-binary pattern for testability — when a script depends on an external command (`code`, `python`, `docker`), the round-trip tests need to exercise the command's behavior without touching the live system. v0.6 introduced `tests/vscode/fixtures/mock-bin/code` — an extension-less Bash script that mimics `code --list-extensions` and `code --install-extension` via a `MOCK_STATE_FILE` env var. The test runner sets `PATH="${MOCK_BIN}:${PATH}"` so the mock takes precedence over the real binary. Same pattern works for any sufficiently-narrow CLI surface. Prerequisite: the script under test must accept a `--file <path>` (or similar) override so it doesn't hardcode-write to the live tracked file. v0.6 added that flag to `sync-vscode.sh` as part of the /review fix. Pattern: when adding a CLI script that touches external state, build in a state-file override flag from the start so future mock-based testing is cheap. | dev-platform | active |
| 2026-05-11 | Per-Spec-Phase branching is the default, but small Roadmap Phases (1 Spec Phase fitting in <200 LOC diff) can ship as a single branch/PR. v0.6 had nominally 3 Spec Phases (Tracking / Deploy+Test / Wire-up) but only 6 Changes totaling ~250 LOC — splitting into 3 PRs would have been per-PR ceremony exceeding per-PR content. The spec explicitly called out the deviation: "Single feature branch ... single PR (no Per-Spec-Phase strategy needed)." Result: 1 PR merged cleanly. Pattern: per-Spec-Phase is appropriate when Spec Phases are loosely coupled OR each Phase exceeds ~150 LOC; otherwise a single branch is cheaper. Future /plan agents should call out the strategy choice in the spec's Design Philosophy or Implementation Order section so /code knows whether to create one branch or many. | dev-platform | active |
