# dev-platform Roadmap

Spec sequence for the `teelr/dev-platform` repo. Each entry is a Roadmap Phase — its detailed Spec lives under `tasks/`, and each maps to a GitHub Milestone in the repo with the same `v<MAJOR>.<MINOR>: <Title>` name.

**Version semantics:** `v0.x` = pre-1.0 build-out (the dev-platform standards + tooling are still mutable). `v1.0` cuts when v0.1–v0.9 all ship and dev-platform is feature-complete for both solo and 2–10 dev teams. Subsequent enhancements become `v1.1`, `v1.2`, …; breaking workflow changes become `v2.0`. Projects that vendor dev-platform can pin to a specific version for stability.

- **v0.1: Foundation** *(complete — 2026-05-08, `tasks/dev-platform-foundation-spec.md`)* — repo restructure, top-level dir contracts, install / uninstall / verify scripts. Migrated `~/.claude/{commands,skills/WORKFLOW_MANUAL.md,settings.json}` into the repo as the source of truth; live cutover executed against `~/.claude/`.
- **v0.2: Global Claude + Hooks Coverage** *(complete — 2026-05-09, `tasks/dev-platform-global-claude-and-hooks-spec.md`)* — closes the gateway-policy gap: tracks `~/.claude/CLAUDE.md` as `dev/settings/claude-global.md`, ships first hook script (`hooks/post-tool-heartbeat.sh`) plus the `settings.json` hooks-block wiring. 13 symlinks now flow from `dev/` → `~/.claude/`. The heartbeat is the data source v0.5 will consume.
- **v0.3: Project Scaffolding** *(complete — 2026-05-10, `tasks/dev-platform-scaffolding-spec.md`)* — three starter templates (`go-service`, `python-agent`, `next-frontend`) under `scaffolding/`, orchestrator at `scripts/new-project.sh`, conversational Q&A pattern at `docs/NEW-PROJECT.md`, Scope-rule scaffolding carve-out inline in `dev/CLAUDE.md`. Assistant runs the scaffold; user describes the project. v0.6 (VSCode user-profile + extensions list) deferred to its own spec.
- **v0.4: Testing** *(complete — 2026-05-10, `tasks/dev-platform-testing-spec.md`)* — `scripts/gate_fast.sh` orchestrator + 5 auto-discovered test suites under `tests/` (hook fixtures, command frontmatter validator, taxonomy self-test, install round-trip, scaffold smoke). 42 mechanical checks aggregate to a single PASS/FAIL in < 1s. Replaces conversation-derived gate-fast with a runnable script every spec from here on rides. Auto-discovery: any new `tests/<suite>/*.sh` is picked up without editing the orchestrator. Deferred to future small specs: `gate_full.sh` (per-template builds), pre-commit git hook, performance benchmarks.
- **v0.5: Monitoring** *(complete — 2026-05-11, `tasks/dev-platform-monitoring-spec.md`)* — workflow telemetry: 4 hook collectors (SessionStart, UserPromptSubmit, PreToolUse, PostToolUse) + `gate_fast.sh` self-instrumentation emit JSONL events to `~/.claude/dev-platform-telemetry.log`. The Python aggregator at `monitoring/aggregator.py` parses both legacy v0.2 text format AND the new JSONL, computes 5 metrics (gate_pass_rate, code_retries, review_count, tool_duration, events_per_project), produces markdown or JSON. CLI entry: `./scripts/report.sh [daily|weekly|all] [project] [--json]`. Centralized emitter `hooks/_emit_event.py` eliminates `project_for()` duplication; metrics catalog at `monitoring/metrics.md` documents each metric's limitations honestly. Shipped via 4 PRs (per-Spec-Phase strategy): Phase 1 (schema + storage), Phase 2 (collectors), Phase 3 (aggregation + reporting), Phase 4 (tests + acceptance + docs). 10-assertion fixture suite at `tests/monitoring/` auto-discovered by `gate_fast.sh` exercises every code path including malformed-line skipping and degraded-event exclusion.
- **v0.6: VSCode Coverage (Server-Side)** *(complete — 2026-05-11, `tasks/dev-platform-vscode-coverage-spec.md`)* — VSCode Remote-SSH server-side extension management. `extensions/vscode/server-extensions.json` tracks the installed extension list (43 entries at first capture); `scripts/sync-vscode.sh` is the bidirectional helper with `capture`/`deploy`/`diff` modes plus a `--file <path>` override for testability; `scripts/install.sh vscode` (graceful skip when `code` CLI absent) reinstalls the full list on a fresh server. `tests/vscode/` ships a 10-assertion fixture suite with a mock `code` binary (`fixtures/mock-bin/code`) that exercises capture/deploy round-trips without touching live state. **Client-side coverage** (laptop's `settings.json`, `keybindings.json`, snippets, theme) is explicitly deferred to a future v0.6b spec per the Option C decision recorded 2026-05-11 — the Remote-SSH client/server split with per-OS install paths warrants its own design pass when there's appetite for it.
- **v0.7: Team Enablement** *(complete — 2026-05-11, shipped across 4 PRs `tasks/dev-platform-team-enablement-spec.md` + 3 standalone Phase specs)* — scaled dev-platform from solo to 2–10 dev team on GitHub: (1) `check_spec_taxonomy.sh` extended to scan ROADMAP.md + planning.md for non-conforming Roadmap Phase headers (PR #7); (2) GitHub Actions CI: `.github/workflows/gate.yml` (dev-platform's own gate-fast) + `.github/workflows/taxonomy-check.yml` (reusable workflow consumers `uses:` via `@v0.7` pin) + `extensions/github-actions/dev-platform-gate.yml` (consumer template); branch protection on main requires the `gate-fast` status check (PR #8); (3) workflow-doc extension locking `→ PR → CI → merge → post-merge` into the canonical chain (chore PR #9); (4) GitHub Pages docs site live at <https://teelr.github.io/dev-platform/> with `docs/GLOSSARY.md` (35 terms) + `docs/index.md` (PR #10); (5) `scripts/sync-milestones.sh` for idempotent ROADMAP→Milestones sync, closes v0.7 (PR #11). Release tag `v0.7` cut at `aa1db43`; v0.7 GitHub Milestone closed; `@v0.7` consumer pin resolves cleanly. Linear/Jira tracker sync still deferred until a tracker is chosen.
- **v0.8: Cross-project orchestration** *(complete — 2026-05-11, shipped across 4 PRs)* — fleet-level operations across every project in `monitoring/projects.json`: (1) `scripts/fleet-gate.sh` sweeps `gate_fast` across all enabled projects, parallel execution with per-project timeout + telemetry (PR #12); (2) `monitoring/fleet_dashboard.py` + `scripts/fleet-status.sh` per-project state aggregator — last-commit recency, branch, uncommitted count, taxonomy compliance, consumer-template adoption flag (PR #15); (3) `scripts/fleet-install-template.sh` opt-in per-project install of the dev-platform-gate consumer template into `<project>/.github/workflows/dev-platform-gate.yml` — the first and ONLY mutating Phase of v0.8, governed by a narrow Scope-rule carve-out in `CLAUDE.md` ("Exception — v0.8 fleet orchestration (mutating subset)"); dry-run default, refuse-to-clobber, `--pin` override (PR #16); (4) `monitoring/fleet_pins.py` + `scripts/fleet-pins.sh` per-project pin tracker — anchored regex extracts each consumer's `@vX.Y` pin from their `dev-platform-gate.yml`, compares against latest dev-platform release via `gh api`, classifies into 6 staleness states (Phase 4 PR — closes v0.8 with the `v0.8` release tag cut at post-merge; consumer-template default-pin bump `@v0.7` → `@v0.8` ships as a follow-up chore PR after the tag exists). All four Phases shipped under the v0.8: Cross-project orchestration Milestone. Deliberate scope expansion beyond "manage one dev environment" — pre-v0.8 the Scope rule barred project-touching work; v0.8 walks that back for *read-only* and *opt-in correction* operations only (still no per-project feature work).
- **v0.9: Migration tooling** *(complete — 2026-05-11, `tasks/dev-platform-migration-spec.md`)* — Phase 1 renamed 3 legacy `R<N>-` spec files in `tasks/` to clean descriptive names (PR #19). Phase 2 ships CLAUDE.md v0.9 Scope carve-out + `scripts/migrate-workflow-chain.sh` (6 sed patterns, dry-run default, `--apply` opt-in, idempotency guard) + `scripts/audit-project-drift.sh` (read-only per-project chain + taxonomy + CLAUDE.md drift report, markdown or JSON output) + 12-assertion `tests/migration/run.sh` auto-discovered by gate_fast.sh. Gate at 143 PASS. Cross-project chain migration (kermit, kermit-pa, atlas) runs as post-merge: `./scripts/migrate-workflow-chain.sh --project <name> --apply` from each project's own session.
- **v1.0: Feature-complete for solo + team** *(complete — 2026-05-12, PRs #23 + #24)* — capstone release; no new features, documentation accuracy pass only. Fixes stale references accumulated since v0.6: `docs/index.md` "Latest release" section + workflow chain; `README.md` scaffolding row + `install.sh` categories + v0.9 ship mention + gate count; `ROADMAP.md` v1.0 stub → full description; `planning.md` in-flight block. Post-merge: `scripts/sync-milestones.sh --apply` verifies Milestone state; `gh release create v1.0` cuts the stable tag; v1.0 GitHub Milestone closed; consumer-template default-pin bumped `@v0.8` → `@v1.0` (chore PR after tag exists). Subsequent enhancements become `v1.1`, `v1.2`, …; breaking workflow changes become `v2.0`.

- **v1.1: Remote Enforcement** *(complete — 2026-05-16, PR #25)* — encodes expected git origin URL and per-repo identity policy for every owned project in `monitoring/remotes.json`; `scripts/verify-remotes.sh` diffs actual vs. expected and exits 1 on drift; wired into `scripts/verify.sh` so it surfaces in the gate's live deploy check. Covers 14 owned projects across two GitHub accounts (`teelr` and `teelr129` / Osigin-LLC). Gate at 153 PASS (+10 from `tests/remote-verify/`). Post-merge: `scripts/sync-milestones.sh --apply`; `gh release create v1.1`; v1.1 GitHub Milestone closed; consumer-template default-pin bumped `@v1.0` → `@v1.1` (chore PR after tag exists).

- **v1.2: Pre-commit Git Hook** *(complete — 2026-05-19, `tasks/dev-platform-pre-commit-hook-spec.md`)* — universal opt-in pre-commit hook converting the /gate-fast-before-commit rule from documentation to mechanical enforcement. `shell/git-hooks/pre-commit` looks for `scripts/gate_fast.sh` at the repo root, no-ops where absent, refuses commits on FAIL otherwise; bypass via `SKIP_GATE_FAST=1` env var for WIP commits on private branches. New `git-hooks` install category symlinks the hook into `~/.claude/git-hooks/`; users activate per-repo via `git config core.hooksPath ~/.claude/git-hooks`. 5-assertion `tests/git-hooks/` suite covers the cross product of (no-gate / passing / failing) × (default env / `SKIP_GATE_FAST=1`) + install integration. Gate at 158 PASS (+5 from `tests/git-hooks/`). v0.4 testing-spec's other deferred items (`gate_full.sh`, perf benchmarks) remain deferred per the "defer until evidence" ruling — no lessons.md entry reports either gap biting. Post-merge: `scripts/sync-milestones.sh --apply`; `gh release create v1.2`; v1.2 GitHub Milestone closed; consumer-template default-pin bumped `@v1.1` → `@v1.2` (chore PR after tag exists).

- **v1.4: Concurrent Dev Isolation** *(complete — 2026-06-23, `tasks/dev-platform-concurrent-dev-isolation-spec.md`)* — lets more than one Claude Code chat run against the same project without tripping over each other. Two layers: (1) **files and branch** — `/code` starts opted-in projects in their own `.claude/worktrees/<branch>` via the harness `EnterWorktree` tool (instead of `git checkout -b` in a shared tree), with `shell/worktree/link-deps.sh` symlinking the project's heavy git-ignored paths (`.env`, `node_modules`) in per a committed `.claude/worktree-deps` manifest; `/merge` tears the worktree down with `ExitWorktree` + `git worktree remove`. (2) **shared app/database** — chats take turns: `shell/worktree/gate-lock.sh` provides a `with_gate_lock` flock helper (lockfile in the shared git common dir) so concurrent gates serialize the backend stop/restart instead of fighting over the port. dev-platform's own `gate_fast.sh` adopts the lock around its live `~/.claude/` verify as the reference. New `worktree` install category; opt-in per project via the marker file; default (no marker) keeps `git checkout -b` unchanged. Does NOT provide per-chat live apps (per-chat ports + databases) — explicitly out of scope. Plain-English how-to + per-project adoption runbook in `docs/CONCURRENT-DEV.md`. Gate at 172 PASS (+9 from `tests/worktree/`). Post-merge: per-project adoption (Kermit, Kermit PA, Keystone, SQRL) runs from each project's own session; `sync-milestones.sh --apply`; `gh release create v1.4`; v1.4 Milestone closed; consumer-template default-pin bumped `@v1.3` → `@v1.4` (chore PR after tag exists).
- **v1.6: Local Settings Isolation** *(complete — 2026-06-28, `tasks/dev-platform-local-settings-isolation-spec.md`)* — stops Claude Code's "always allow" permission grants from polluting the tracked (public) repo. Root cause: `~/.claude/settings.json` was a symlink into `settings/settings.json`, so every runtime grant wrote straight through into version control (months of recurring drift). The Claude Code docs don't document where grants are written or how to redirect them (claude-code-guide research, all "NOT DOCUMENTED"), so the fix is structural: `settings.json` is now **merge-deployed as a real local file** (not a symlink) via `scripts/merge_settings.py` (unions the baseline's permission arrays into the live `~/.claude/settings.json`, baseline wins on config keys) — grants accumulate locally and never reach the repo; `settings.local.json` is seeded once from a secret-free `settings/settings.local.json.example`. `verify.sh` gains a real-file-superset check (and flags a regression if `settings.json` is ever a repo symlink again); `claude-global.md`/`keybindings.json` stay symlinked (not runtime-writable). **Scope honesty:** stops *future* pollution; does NOT prune the existing 113-entry baseline; the union-merge can add but not retract a permission. Surfaced (but did not fully remediate) that `settings/settings.local.json` carried plaintext DB passwords — now replaced by a secret-free `.example` + local-only live file. No post-merge step.
- **v1.5: Cross-Repo Comms Migration** *(complete — 2026-06-28, `tasks/dev-platform-comms-migration-spec.md`)* — finishes the cross-repo comms migration started in #33 (inbound: consumer→dependency asks go upstream as GitHub issues). Ships the parts that live in dev-platform: (1) the **outbound standard** in `docs/CROSS-REPO-COMMS.md` — the dependency announces versions via GitHub Releases + release notes, consumers watch the repo / run Dependabot/Renovate, the `HARNESS_REPLIES_INBOX.md` broadcast-doc relay is deprecated; (2) a copy-paste **Dependabot consumer template** at `extensions/github-actions/dependabot-consumer-template.yml`; (3) `monitoring/comms-consumers.json` (consumer → upstream repo / label registry) + `scripts/setup-consumer-labels.sh` (idempotent `gh label create`, dry-run default) for the `consumer:*` triage labels; (4) the **delivery checker** `scripts/check-comms-delivery.sh` + `monitoring/comms_delivery.py` — scans every post-2026-06-28 `communique-to-<dep>-*` file and FAILs if it doesn't link a live upstream issue (the 2026-06-28 "filed but never delivered" failure mode). The checker is standalone (gh network calls, maybe-uncloned paths), not in `gate_fast`; its offline mock-`gh` suite is. **Scope honesty:** dev-platform ships the standard + template + tools; the harness actually cutting Releases and each consumer enabling Dependabot are post-merge per-repo coordination, not performed here. Gate at 190 PASS (+18 from `tests/comms-labels/` + `tests/comms-delivery/`). Post-merge: `setup-consumer-labels.sh --apply` against `teelr/kermit-harness`; harness stops relaying broadcast docs and cuts Releases from its own session; consumers adopt the Dependabot template from their own sessions; `sync-milestones.sh --apply`; `gh release create v1.5`; v1.5 Milestone closed; close issue #34.
- **v1.3: Mandatory Review Gate** *(complete — 2026-06-07, `tasks/dev-platform-mandatory-review-spec.md`)* — promotes `/review` from optional ("for risky/large changes") to a **mandatory** step in the canonical chain, between `/code` and `/gate fast`; new chain: `/plan → /code → /review → /gate fast → commit → push → /pr → CI → /merge → post-merge`. Second half (option 3): `/code` gains an explicit "Adversarial Self-Review" step (re-read your own diff as a hostile reviewer before reporting), distinct from the independent `/review` gate. Lockstep updates across the authoritative contract (`CLAUDE.md`, `settings/claude-global.md`), commands (`commands/code.md`, `commands/review.md`), published docs (`docs/index.md`, `skills/WORKFLOW_MANUAL.md`), the three scaffolding READMEs, and the migration tooling (`migrate-workflow-chain.sh` + `audit-project-drift.sh` now detect the review-less chain via `/code → /gate` and insert `/review`). Gate at 163 PASS (+5 from new `tests/migration/` assertions). Critical hazard navigated: the audit greps `CLAUDE.md` for `/code → /gate`, so no bare review-less chain string may appear in dev-platform's own `CLAUDE.md` (else self-flag) — verified `audit --project dev-platform` reports CLEAN. Post-merge: re-run `migrate-workflow-chain.sh --apply` against each consumer project to insert `/review` into their chain line; `sync-milestones.sh --apply`; `gh release create v1.3`; v1.3 Milestone closed; consumer-template default-pin bumped `@v1.2` → `@v1.3` (chore PR after tag exists).
- **v1.7: Plan-Time Isolation** *(complete — 2026-07-01, PR #41)* — moves branch/worktree creation from `/code` time to `/plan` time: `/plan` now derives the feature slug from `$ARGUMENTS`, creates the branch (`git checkout -b`) or worktree (`EnterWorktree`, for opted-in multi-chat projects) right when the user decides what to build, and renames the current tmux window to the slug if `$TMUX` is set — so two concurrent `/plan` sessions never collide writing `tasks/*.md` or `planning.md` on a shared branch. `/code`'s own branch-creation step becomes the fallback for hand-authored specs. Adds `shell/new-session.sh`, a personal CLI+tmux helper (`<project-dir> <branch-name> [window-name]`) for deliberately starting a second, fully parallel worktree + tmux window + `claude` session when the branch name is already known upfront. `/review` caught one real bug before commit: the worktree-mode instructions referenced capturing `MAIN=$(git rev-parse --show-toplevel)` only in a trailing note, after the point where `EnterWorktree` would have already re-rooted the session — fixed to capture `MAIN` as an explicit first action, matching `/code`'s existing ordering.
- **v1.8: Missing-Chain Detection** *(complete — 2026-07-04, `tasks/audit-project-drift-spec.md`)* — closes the drift-audit blind spot where `audit-project-drift.sh` reported a `CLAUDE.md` that omits the workflow chain as `CLEAN`, indistinguishable from a correct one (surfaced when SQRL joined the fleet registry and read `CLEAN` despite documenting no chain at plan time). Adds a fourth chain state, `MISSING_CHAIN` — file present, no old-chain drift pattern, no canonical `/code → /review` anchor — counted toward the drift summary with a distinct remediation hint: `migrate-workflow-chain.sh` rewrites an existing chain and cannot insert an absent one (verified it no-ops on a chain-less file), so a missing chain is a manual add from the project's own session. Detection order is load-bearing (DRIFT wins before the canonical check); the anchor is deliberately narrow (`/code → /review`, not bare `/review`) so unrelated prose can't rescue a chain-less file into `CLEAN`. +4 assertions in `tests/migration/run.sh` (21 total). Follow-on shipped in v1.9: `docs/PROJECT_CLAUDE_TEMPLATE.md` + the scaffolding templates didn't seed the chain, so scaffolded projects would start `MISSING_CHAIN`.
- **v1.9: Template Chain Seeding** *(complete — 2026-07-04, `tasks/template-chain-seeding-spec.md`)* — the companion to v1.8: seeds the canonical dev workflow chain into the project `CLAUDE.md` templates so a freshly scaffolded project is born audit-CLEAN instead of reading `MISSING_CHAIN`. Adds a `## Development Workflow` section (chain + `gate_fast.sh` note) to the three `scaffolding/*/CLAUDE.md` templates and `docs/PROJECT_CLAUDE_TEMPLATE.md`; `tests/scaffold/run.sh` gains an end-to-end guard (+3 assertions) that runs the v1.8 audit against each freshly scaffolded project and requires `CLEAN`. The chain line is a required project-level workflow declaration (what real projects carry), not a "don't duplicate rules" violation — the doc template carries a one-line HTML comment saying so.
- **v1.10: Roadmap-Phase Completion** *(complete — 2026-07-07, `tasks/phase-completion-spec.md`)* — closes issue #50: the canonical chain was entirely Change-scoped, so nothing owned Roadmap-Phase completion and phase docs + GitHub milestones drifted out of sync every phase (kermit-v3's v0.1–v0.3 milestones all left `open`; dev-platform's own v1.9 read "in flight" weeks after merge). Makes the phase-close a **standard** (non-bespoke) post-merge sub-step, documented in the three workflow-contract files (`CLAUDE.md`, `settings/claude-global.md`, `skills/WORKFLOW_MANUAL.md`) and surfaced by `/merge`'s Step 6 report when a merge completes a phase: mark the phase complete in `ROADMAP.md` + `planning.md` and close its milestone (handling "closed by scope decision" the same as "closed by code"). Ships the mechanical backstop `scripts/check-phase-milestones.sh` — a standalone `gh` detector (NOT in `gate_fast`, same rationale as `check-comms-delivery.sh`) that flags any milestone left `open` with 0 open issues and ≥1 closed; +8 assertions in a new `tests/phase-milestones/` offline mock-`gh` suite. **Honest scope:** the detector catches milestones whose assigned PRs are all closed; it cannot see a phase whose PRs were never assigned to a milestone (that gap is covered by the post-merge step + `/pr`'s assignment). Fleet-wide sweep and an offline planning-vs-milestone cross-check are deferred future items.
- **v1.11: Roadmap-Version Claim Guard** *(complete — 2026-08-12, PR #60)* — promotes kermit-v3's proven `scripts/claim_roadmap_version.py` + `scripts/check_version_collision.py` into dev-platform so every consumer project (not just kermit-v3) gets protection against two concurrent `/plan` sessions claiming the same Roadmap Phase version number. Wires the atomic claim into `/plan` Step 2 (replacing a "read `planning.md`, assume it's free" pattern), retires the matching unguarded `gh api ... POST` fallback in Step 6, and folds `check_version_collision.py` into the shared `taxonomy-check.yml` reusable CI workflow so every consumer with `dev-platform-gate.yml` installed gets the check on their next pin bump with zero local wiring. Adds a 10-assertion fixture suite (`tests/version-collision/`) using a real local git remote + mock `gh`. Gate at 222 PASS (+10). Single-PR spec — deferred to future specs: the `planning.md`/`ROADMAP.md`/`tasks/lessons.md` doc-merge-collision redesign, a matching reservation mechanism for `tasks/lessons.md`'s hand-incremented lesson numbers, and the unconfirmed CI-dispatch-miss symptom from kermit-v3 PR #328/#331. Released `v1.11`, milestone #21 closed. **Follow-on same week:** its regex only matched kermit-v3's heading-form `ROADMAP.md` convention, silently providing zero protection against dev-platform's own list-form convention — fixed in v1.12.
- **v1.12: Version Guard List-Form Roadmap Support** *(complete — 2026-08-12, PR #61, `tasks/version-guard-list-form-support-spec.md`)* — fixes the gap discovered during v1.11's own post-merge dogfooding: `claim_roadmap_version.py` and `check_version_collision.py`'s Roadmap Phase version regexes only matched the heading form (`## v<N>.<M>: <Title>`, kermit-v3's convention), never dev-platform's own list form (`- **v<N>.<M>: <Title>** ...`) — meaning the `version-collision` CI job added in v1.11 silently passed on every PR against dev-platform, providing zero real protection. Fix mirrors `scripts/check_spec_taxonomy.sh`'s own proven dual-form pattern (`^(- \*\*|## )`) instead of inventing a new one. Test suite re-pointed at list-form as the primary fixture (closing the actual gap) plus 2 new assertions proving heading-form still works (dual-form, not a swap) — 12 assertions total (+2). Gate at 224 PASS. Live-dogfooded against dev-platform's own repo. Released `v1.12`, milestone #23 closed, consumer-template default pin bumped `@v1.10 → @v1.12` (skipping the broken `@v1.11` default entirely — see this chore).
- **v1.13: Configurable Roadmap Path** *(complete — 2026-08-13, PR #65, `tasks/configurable-roadmap-path-spec.md`)* — closes [issue #64](https://github.com/teelr/dev-platform/issues/64), filed by Keystone's session: `check_version_collision.py` and `check_spec_taxonomy.sh` both hardcoded `ROADMAP.md` at the repo root, silently no-op'ing for Keystone (roadmap lives at `docs/roadmap.md`, no `planning.md` at all) — a confident-looking PASS/"nothing to check" that never actually checked anything. Keystone's first fix attempt (symlinking root `ROADMAP.md` to the real file) made it worse: `git show origin/main:ROADMAP.md` doesn't dereference symlinks, producing false `COLLISION` failures against their own milestone history (reverted same night). Adds a `ROADMAP_PATH` env var (default `ROADMAP.md`, fully backward-compatible) to all three scripts that read the roadmap — including `claim_roadmap_version.py`, not named in the issue but with the identical bug, fixed alongside the other two rather than leaving a third script silently broken. Documents the symlink footgun explicitly so no other consumer rediscovers it the hard way. Every new test proves both the override works and default behavior is unchanged. Gate at 228 PASS (+4). Released `v1.13`, milestone #24 closed.
- **v1.14: Gate-Fast Docs-Only Diff Skip** *(complete — 2026-08-16, PR #69, `tasks/gate-fast-docs-only-skip-spec.md`)* — closes [issue #68](https://github.com/teelr/dev-platform/issues/68): ships a reusable docs-only-diff detector (`scripts/lib/docs_only_diff.sh`) wired into dev-platform's own `scripts/gate_fast.sh` as the reference implementation — skips the auto-discovered test-suite loop when a diff vs. `main` touches only inert documentation, while taxonomy/syntax/secrets/live-verify lift checks always still run. Proven live on kermit-v3 (PR #367) and Keystone's own gate the same day; propagates the pattern per dev-platform's "projects extend, don't replace" quality-gate contract. Ships as a **documented pattern** (new `docs/RULE_RATIONALE.md` section) rather than a shared script, since `gate_fast.sh` has no runtime distribution mechanism (unlike the CI YAML template). `/review` caught a real gap: bash's `*.md` matches any `.md` file at any depth, which would have wrongly classified `commands/*.md` as docs even though `tests/commands/frontmatter.sh` validates those files live — fixed by narrowing dev-platform's own allowlist explicitly instead of trusting the shared helper's generic default. Gate at 235 PASS (+7). Released, milestone #25 closed. Post-merge filed handoff issues on `keystone_prototype`, `SQRL`, `OPIE`, `kanban` (+ a lighter FYI on `keystone`, which already had an equivalent) so each ports the pattern from its own session.
- **v1.15: Post-Merge Change Summary** *(complete — 2026-08-20, `tasks/post-merge-change-summary-spec.md`)* — the standard post-merge Roadmap-Phase-completion sub-step (v1.10) only fires when a merge happens to close the last Change of a phase, so most merges (mid-phase Changes, standalone chores/fixes) shipped with zero post-merge summary. Adds a **second** standard (non-bespoke) post-merge sub-step, unconditional on phase completion: every `/merge` invocation ends its report with a short **Problem/Opportunity → What shipped** summary — chat-report only, no new file, no CHANGELOG, no GitHub Release/PR comment. Derived from context already available at merge time via a 3-tier fallback: (1) the lines the merge added to `planning.md`'s "Recently shipped" section (the prose `/code` already wrote pre-commit, reusing it rather than regenerating), (2) the merged spec's Problem framing + Change titles if `planning.md` wasn't touched, (3) the PR title/body via `gh pr view` if no spec was touched at all — the case that most needed this, since it previously got no post-merge output whatsoever. `commands/merge.md` Step 7 restructured so summary generation runs BEFORE the existing "no spec touched → stop" early exit, which previously skipped it for exactly that case. Lockstep documentation across `CLAUDE.md`, `settings/claude-global.md`, `skills/WORKFLOW_MANUAL.md` (same shape as v1.10). No new script or test suite — instruction-file change only, same class as v1.7's Plan-Time Isolation. Does NOT touch the chain string itself or `audit-project-drift.sh`'s chain detection — the new sub-step lives entirely inside the existing `post-merge` box.
- **v1.16: Fold Review Into Code** *(complete — 2026-08-20, `tasks/fold-review-into-code-spec.md`)* — `/review` has been a mandatory chain step since v1.3 with zero exceptions in this repo's history: every `/code` turn is followed by an explicit, separately-invoked `/review`. Applies the same fold `/merge`→`post-merge` already got in v1.10/v1.13: `/code` now runs `/review`'s full procedure itself, same turn, no separate invocation — new `commands/code.md` Step 9 points at `commands/review.md` Steps 1-5 as the single source of truth rather than duplicating them, then the old "Report" step renumbers to Step 10 and ends with "Ready for `/gate fast`" instead of "Ready for `/review`." The STOP boundary does NOT move to after `/gate fast` (explicit user requirement) — it stays immediately after `/review` finishes, exactly where it sits today; only the `/code`→`/review` boundary folds, not `/review`→`/gate fast`. `settings/claude-global.md` now documents two `Exception:` paragraphs (code→review, merge→post-merge) instead of one; its stop-trigger list drops `/code`, mirroring how `/merge` was already absent from that list once post-merge folded into it. `/review` remains fully invokable standalone for the recovery case (re-review after manual edits, or resuming an interrupted `/code` session) — its own review contract (SECURITY/BUG/COMPLIANCE/QUALITY auto-fixed, ARCHITECTURE surfaced, report format) is completely unchanged. The chain string itself (`/plan → /code → /review → /gate fast → ...`) never changes anywhere it appears, so `scripts/migrate-workflow-chain.sh` and `scripts/audit-project-drift.sh` — both of which grep for the literal `/code → /review` substring — needed zero changes; confirmed `audit-project-drift.sh --project dev-platform` still reports CLEAN. One spec deviation: `commands/review.md`'s frontmatter `description` as originally drafted exceeded the 200-char limit `tests/commands/frontmatter.sh` enforces — shortened while preserving the same dual-invocation meaning. No new script or test suite. Gate at 235 PASS (unchanged — no code/test files touched).
- **v1.17: Duplicate Number Check** *(complete — 2026-08-21, `tasks/duplicate-number-check-spec.md`)* — closes [issue #75](https://github.com/teelr/dev-platform/issues/75): kermit-v3 PR #481 hit a real collision when two in-flight PRs both claimed `tasks/HARNESS_HANDOFF_QUEUE.md`'s Ask #51 (merged with zero text conflict — different line positions) and `tasks/lessons.md`'s L60 (a real git conflict, but resolving it correctly was a manual judgment call). Ships `scripts/check_duplicate_numbering.sh` — a generic, table-SCOPED checker (verified live against kermit-v3's real file: the same number legitimately repeats across independently-numbered tables today, e.g. `#39` and `#37` each appear twice in two different tables — a naive whole-file scan would falsely flag both) plus a global `## L<N> —` duplicate check for lessons.md. Verified the three consumer projects do NOT share one file shape before assuming otherwise: kermit-v3 has 5 `#`-headed queue tables, Keystone has 4 (missing kermit-v3's specific Ask table), kermit-pa's queue file has zero `#`-headed tables at all — only its lessons-L# convention matches. Wired into dev-platform's own `gate_fast.sh` as the reference implementation (a graceful no-op here — dev-platform has neither convention); documented in `docs/RULE_RATIONALE.md`'s Kermit-Specific Rules section (not `CLAUDE.md`'s Gate Tiers, since this is consumer-specific, not universal — mirrors how `check_version_collision.py` gets no `CLAUDE.md` mention either). 8-assertion `tests/duplicate-numbering/` suite, including the load-bearing regression guard proving same-number-different-table reuse is NOT flagged. Gate at 244 PASS (+9). Released, milestone #28 closed. Post-merge filed handoff issues on kermit-v3/Keystone/kermit-pa (kermit-pa's noting only the lessons-L# pass applies today).
- **v1.18: Tmux Session Helper** *(complete — 2026-08-28, `tasks/tmux-session-helper-spec.md`)* — Claude Code sessions died several times a day: the VS Code remote server runs with `--enable-remote-auto-shutdown` and exits five minutes after the last client disconnects, and its exit destroys the pty host, killing every `claude` running in a terminal. Ships `shell/profile.d/claude-tmux.sh`, defining `cc` — attach-or-start a tmux session per project, so the session lives in a server owned by init rather than inside the VS Code process tree. Not an alias: it resolves `claude` to an absolute path (a running tmux server doesn't inherit the caller's `PATH`), falls back to a login shell when claude exits so quitting doesn't destroy the session, and strips `.`/`:` from session names (tmux target syntax) so worktree dirs like `v0.37+phase-1` work. `cc` deliberately shadows `/usr/bin/cc`; builds are unaffected because bash functions aren't inherited by `execvp`, and `command cc` still reaches the compiler. Also closes a deployment gap `shell/README.md` had been documenting since v0.1: there was no `shell` install category at all, so `shell/new-session.sh` (v1.7) was unreachable and the README's `~/.shell-platform/` deployment claim was false. Adds `install_shell` → `~/.claude/profile.d/` (depth 2, so `uninstall.sh`'s `find -maxdepth 2` sweep actually removes it — a nested `~/.claude/shell/profile.d/` would survive uninstall with `.bashrc` still sourcing the repo), the matching `verify.sh` drift check, and `shell/` in `gate_fast.sh`'s bash-syntax roots (which had never covered `new-session.sh`). 18-assertion `tests/shell/` suite running against a `TMUX_TMPDIR`-isolated server with a stub `claude`, including the depth-2 uninstall regression guard. Three defects fixed relative to the drafted material: the new file was silently gitignored (`.gitignore`'s `shell/**` — `!shell/*.sh` doesn't reach subdirectories), the deploy path was one level too deep for uninstall, and `cc --kill`/`cc -n` with no argument aborted under `set -u` via unguarded `$2`. Two spec errors found and corrected at `/code` time: `git check-ignore -v` exits 0 on a negation match (wrong assertion), and the deploy had to move from post-merge into `/code` because the new verify check runs in `gate_fast` before commit. Gate at 262 PASS (+18).
- **v1.19: Parallel Coding Sessions** *(complete — 2026-08-28, `tasks/parallel-coding-sessions-spec.md`)* — starting a second, third or fourth Claude Code session on one project meant inventing a tmux session name (`cc -n review kermit-v3`) and remembering it, because a plain `cc <project>` reattaches to the first session rather than starting another. Adds `cc --new`: the next free numbered session (`kermit-v3-2`, `-3`, `-4`) in the same directory, with `cc -n kermit-v3-2` as the way back — which needed zero new code, since `-n` already attaches when the session exists. Numbering takes the LOWEST free suffix rather than a counter, so killing `-2` frees that name again. **`--new` deliberately does no git work.** The obvious design — a worktree per session so each gets its own branch — was dropped at plan time after a sweep of `projects/*/.claude/worktree-deps` showed the five projects real coding happens in (kermit, kermit-pa, kermit-v3, keystone, SQRL) have ALL already opted into worktree mode, where `/plan` Step 2 gives each session its own copy of the repo via `EnterWorktree` and `/merge` Step 5 tears it down. `cc` had nothing to add to that but a free session name. It also cannot add it correctly: a fresh worktree needs the project's heavy git-ignored paths symlinked in, and that list is per-project in a committed `.claude/worktree-deps` manifest a shell function cannot invent — which is exactly why worktree mode is opt-in. For the thirteen branch-mode projects `cc --new` prints a one-line warning that the session shares one working tree and one branch, instead of silently handing over a tree two sessions will fight over. Retires `shell/new-session.sh` (v1.7) — `cc --new` covers "another session here" and `/plan` covers branch creation, so three overlapping ways to start a session collapse to one; it was never deployed by `install.sh` anyway, so there were no users to migrate. Sibling-dir worktrees (`<repo>-2`, the pattern `new-session.sh` used) were ruled out in the spec: `/merge` Step 4 detects worktrees by matching `*/.claude/worktrees/*` and would not tear them down, and its branch-mode path runs `git checkout main`, which fails inside a sibling worktree because `main` is checked out in the primary. +10 assertions in `tests/shell/run.sh` (numbering from -2 with and without a base session, lowest-free reuse after a kill, reattach without duplicating, arg pass-through, both isolation messages, and the `--new`/`-n` conflict). Gate at 273 PASS (+10). Deferred, deliberately: opting dev-platform itself into worktree mode — `scripts/install.sh` symlinks `~/.claude/*` at repo paths, so installing from a worktree leaves the live deployment pointing into a directory `/merge` later deletes, and `.gitignore` ignores all of `.claude/`. Separate decision, separate spec.
- **v1.20: Concurrent Session Awareness** *(complete — 2026-08-28, `tasks/concurrent-session-awareness-spec.md`)* — v1.19's `cc --new` made two-to-four sessions on one project routine, but nothing in `commands/` knew that. A `/dev` run in kermit-v3 found a second live session, improvised a warning out of raw `ps` output, and got it wrong: it told the user not to run `./scripts/gate_fast.sh` while the other session was building — when kermit-v3 wires the take-turns lock at `scripts/gate_fast.sh:163` and a concurrent gate simply blocks on `flock` and waits its turn. The advice turned a solved problem back into a scary one, and because it was improvised rather than instructed, the next run would improvise something different. There was no wrong instruction to correct — `commands/dev.md` had no concurrent-session step at all, and nothing in `commands/` or `skills/` mentioned `gate-lock.sh`. Ships `scripts/check-concurrent-sessions.sh`, a read-only detector in the same shape this repo already used twice for this class of problem (`check-phase-milestones.sh` v1.10, `check_duplicate_numbering.sh` v1.17): standalone, always exits 0, deliberately NOT wired into `gate_fast.sh`. It reports live sessions per worktree, whether worktree mode isolates them, that the app and database stay shared regardless, and whether a concurrent `/gate fast` takes turns. `/dev` gains a step that runs it and reports its lines as facts, plus two rules forbidding the improvisation. **Three findings, each a false answer waiting to happen.** (1) kermit-v3 has no `with_gate_lock` call at all — it sources `gate-lock.sh` then does `exec 9>"$(_gate_lockfile)"; flock 9` directly, so grepping for the function name (the obvious implementation) reports it unlocked; dev-platform, keystone and kermit-v3 each wire the lock differently and the pattern has to match all three. (2) The main checkout is a string prefix of every worktree under it, so first-match-wins labels every worktree session `main`. (3) A missing `/proc` must report `unknown`, never a count — telling someone they are alone when they are not is the dangerous failure. **One spec deviation, found by running it:** the spec specified path-prefix matching against `git worktree list` roots, which is wrong on this box — every project lives under dev-platform's own repo root, so the first live run reported all four machine-wide sessions as dev-platform's. Membership is now `git rev-parse --git-common-dir`, which distinguishes nested repos and unifies a repo with its worktrees. Self-review added a hop bound to the PPid walk (a malformed fixture cycle would otherwise hang every gate run). 20-assertion `tests/concurrent-sessions/` suite, fully offline against a mocked `/proc` plus real `git init` fixtures, including regression guards for all three findings and the nested-repo case. Gate at 293 PASS (+20). Deferred: wiring the detector into `/code` and `/gate` — `/dev` is the orientation command; the rest is a separate decision.

Canonical roadmap detail (per-Roadmap-Phase exit criteria, dependencies, demo definition) lives in `tasks/dev-platform-roadmap.md` — created when v0.5 starts.
